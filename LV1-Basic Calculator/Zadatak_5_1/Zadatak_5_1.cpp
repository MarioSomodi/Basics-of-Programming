#include "stdafx.h"
#include <iostream>

using namespace std;

int main()
{
	double x, y;
	cout << "Enter the first number : ";
	cin >> x;
	cout << "Enter the second number : ";
	cin >> y;
	double p = (x + y) / 2;
	cout << "Prosjecna vrijednost vasa dva broja je " << p << endl;
	system("Pause");
    return 0;
}

